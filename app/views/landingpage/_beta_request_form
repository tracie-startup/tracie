    <div class="row">
      <div class="col-sm-6 col-sm-offset-3 beta-form">
        <%= form_for(@beta_request) do |f| %>
          <div class="input-group fashion-button <%="group-has-error" if @beta_request.errors.any? %>">

              <%= f.email_field :email, :class => "transparent form-control", :placeholder => "Deine E-Mail-Adresse" %>
              <span class="input-group-btn">
                <%= f.submit :class => "btn btn-default btn-primary", :value =>"Ich wills testen!" %>
              </span>

          </div><!-- /input-group -->
          <div class="description">
            <% if @beta_request.errors.any? %>
              <p class="text-danger">Bitte schau dir noch einmal deine E-Mail-Adresse an. Sie scheint nicht gültig zu sein.</p>
            <% end %>
            Gib uns deine E-Mail-Adresse und wir laden dich in die nächste Testgruppe ein.
          </div>
        <% end %>
      </div>
    </div>
